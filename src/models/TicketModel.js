export default class TicketModel {
    constructor(id,name,numTickets,precios) {
      this.id = id;
      this.name = name;
      this.numTickets = numTickets;
      this.precios = precios;
    }
  }
